export interface HeroSt{
    idS: any;
    firstName: string ;
    name: string;
    age: string;
    description: string;
    action : any;
}

export interface appVersion{
  version: string;
}

export interface PACKAGEVERSION{
  version: string;
}
